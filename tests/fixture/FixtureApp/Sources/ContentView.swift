import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "figure.hiking")
                .imageScale(.large)
            Text("Fixture App")
        }
        .padding()
    }
}
